# 1.7.0 — _in progress_

> **Breaking:** legacy `CMakeSettings.json` removed — build via `CMakePresets.json` only.

## Features
_(none yet)_

## Bugfixes
_(none yet)_

## Deprecations
- Removed `CMakeSettings.json` (legacy Visual Studio CMake integration). Use `CMakePresets.json` instead.
